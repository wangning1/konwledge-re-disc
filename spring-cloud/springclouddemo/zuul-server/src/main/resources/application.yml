eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:7761/eureka/
server:
  port: 6661
spring:
  application:
    name: service-zuul
zuul:
  routes:
    api-ribbon:
      path: /api-ribbon/**
      serviceId: service-ribbon
    api-feign:
      path: /api-feign/**
      serviceId: service-feign
